version: '3'
#######################
# Link services inside API

services:
  backend:
    ports:
      - ${BACKEND_PORT}:${FLASK_DEFAULT_PORT}
  {% if frontend != 'nofrontend' %}
  frontend:
    ports:
      - 80:8080
  {% endif %}

