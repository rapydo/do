""" CUSTOM Models for the relational database """

# Create new models by extending Base:
# from restapi.connectors.sqlalchemy.models import Base
from restapi.connectors.sqlalchemy.models import User
from sqlalchemy import Column, String

# Add (inject) attributes to User
{% if extend -%}
User.my_custom_field2 = Column(String(255))
{% else -%}
User.my_custom_field = Column(String(255))
{% endif -%}
